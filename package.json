{
  "name": "orb-project",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -k -n \"BACKEND,FRONTEND\" -c \"blue.bold,magenta.bold\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend -- --open\"",
    "dev:frontend": "npm run dev --prefix frontend -- --open",
    "dev:backend": "npm run dev --prefix backend",
    "build": "npm run build --prefix frontend",
    "deploy": "npm run build --prefix frontend && gh-pages -d frontend/dist --dotfiles --nojekyll",
    "start": "npm start --prefix backend",
    "install-all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "gh-pages": "^6.3.0"
  }
}
